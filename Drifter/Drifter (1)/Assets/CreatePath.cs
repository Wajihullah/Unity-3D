using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreatePath : MonoBehaviour {
//	LinkedList<int> graph = new LinkedList<int>();

	// Use this for initialization
	void Start () {
		path ();
	}
	
	// Update is called once per frame
	void Update () {
		
	}

	void path () {
//		graph.First = 0;
//		graph.AddAfter = 0;
		for(int i = 0; i < 10 ; i++){
			for(int j = 0; j < 10 ; j++){
//				Debug.Log(graph.AddAfter = Random.Range (0,4));
				//graph [i] [j] = Random.Range (0, 4);
				//Debug.Log (graph [i] [j]);
			}
		}
	}
}
